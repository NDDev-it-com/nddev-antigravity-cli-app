# NDDev builder rule

When NDDev builder context is active, keep public setup-manager implementation
inside the public module and private tests inside the owning validation slice.
Do not introduce live credentials, generated runtime state, deprecated
predecessor implementation paths, or unsupported marketplace formats.
